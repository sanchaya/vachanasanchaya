<% if params[:page] %>
  $('.vachanas_load').append("<%= escape_javascript(render(partial: 'vachanakaara_vachana_results', formats: [:html])) %>");
  var nextPage = <%= @vachanas.next_page || 'null' %>;
  if (nextPage !== null) {
    var newHref = $('.load-more-vachanas').attr('href').replace(/page=\d+/, 'page=' + nextPage);
    $('.load-more-vachanas').attr('href', newHref).show();
  } else {
    $('.load-more-vachanas').remove();
    $('.click_for_more').remove();
  }
<% else %>
  $('.start_letter_vachana_list').html("<%= escape_javascript(render(partial: 'vachanakaara_vachana_list', formats: [:html])) %>");
<% end %>
